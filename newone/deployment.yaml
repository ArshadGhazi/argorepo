apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch8-test
  labels:
    app: branch8-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch8-test
  template:
    metadata:
      labels:
        app: branch8-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
