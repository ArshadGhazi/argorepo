apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch3-test
  labels:
    app: branch3-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch3-test
  template:
    metadata:
      labels:
        app: branch3-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
