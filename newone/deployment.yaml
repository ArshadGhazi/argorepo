apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch4-test
  labels:
    app: branch4-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch4-test
  template:
    metadata:
      labels:
        app: branch4-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
