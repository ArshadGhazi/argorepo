apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch9-test
  labels:
    app: branch9-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch9-test
  template:
    metadata:
      labels:
        app: branch9-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
