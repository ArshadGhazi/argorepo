apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch008-test
  labels:
    app: branch008-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch008-test
  template:
    metadata:
      labels:
        app: branch008-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
