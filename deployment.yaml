apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch002-test
  labels:
    app: branch002-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch002-test
  template:
    metadata:
      labels:
        app: branch002-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
