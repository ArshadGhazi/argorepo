apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch007-test
  labels:
    app: branch007-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch007-test
  template:
    metadata:
      labels:
        app: branch007-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
