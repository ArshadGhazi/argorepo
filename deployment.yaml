apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch005-test
  labels:
    app: branch005-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch005-test
  template:
    metadata:
      labels:
        app: branch005-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
