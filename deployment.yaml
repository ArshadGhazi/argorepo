apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch001-test
  labels:
    app: branch001-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch001-test
  template:
    metadata:
      labels:
        app: branch001-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
