apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch004-test
  labels:
    app: branch004-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch004-test
  template:
    metadata:
      labels:
        app: branch004-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
