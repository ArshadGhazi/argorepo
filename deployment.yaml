apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch-test
  labels:
    app: branch-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch-test
  template:
    metadata:
      labels:
        app: branch-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
