apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch0-test
  labels:
    app: branch0-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch0-test
  template:
    metadata:
      labels:
        app: branch0-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
