apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch006-test
  labels:
    app: branch006-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch006-test
  template:
    metadata:
      labels:
        app: branch006-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
