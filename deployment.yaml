apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch009-test
  labels:
    app: branch009-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch009-test
  template:
    metadata:
      labels:
        app: branch009-test
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
